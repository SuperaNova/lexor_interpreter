name: Publish WebAssembly

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (do not publish package)'
        required: true
        type: boolean
        default: true

jobs:
  publish-wasm:
    name: Build and Publish Wasm to GitHub Packages
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@superanova'

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Build WebAssembly Package
        working-directory: ./wasm
        run: wasm-pack build --scope superanova --target web

      - name: Set package version from git tag
        if: startsWith(github.ref, 'refs/tags/')
        working-directory: ./wasm/pkg
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          npm version "$TAG" --allow-same-version --no-git-tag-version

      - name: Publish to GitHub Packages
        working-directory: ./wasm/pkg
        run: |
          # If it's a tag push, we want to publish (unless dry_run is true on manual trigger)
          DRY_RUN="${{ github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run == 'true' }}"
          if [ "$DRY_RUN" == "true" ]; then
            npm publish --dry-run
          else
            npm publish
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
