[package]
name = "lexor_wasm"
version = "0.1.3"
edition = "2024"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
lexor_core = { path = "../core" }
wasm-bindgen = "0.2"
web-sys = { version = "0.3", features = ["Window"] }
